using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FogOfWar : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        UnitManager.Instance.OnA
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
